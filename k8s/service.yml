apiVersion: v1
kind: Service
metadata:
  name: ucad-cotisations-service
spec:
  type: NodePort
  selector:
    app: ucad-cotisations
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080